# Starter

Run the tests with:

```bash
mvn test
```

The service intentionally returns an order without checking ownership and accepts deletion without authorization. Add tests first, then repair `OrderService.java`.
